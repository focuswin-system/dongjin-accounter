{
  "name": "focus-accounter-server",
  "version": "1.0.0",
  "description": "도니도라 회계관리 백엔드 — Express + MariaDB (멀티테넌트, DB-per-tenant)",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test test/",
    "seed": "node seed.js",
    "setup:db": "node scripts/setup-db.js",
    "setup:db:check": "node scripts/setup-db.js --check",
    "check:db": "node scripts/check-db.js",
    "check:isolation": "node scripts/check-isolation.js",
    "migrate:uploads": "node scripts/migrate-uploads.js",
    "tenant": "node scripts/provision-tenant.js",
    "tenant:delete": "node scripts/delete-tenant.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "mysql2": "^3.22.4",
    "xlsx": "^0.18.5"
  }
}
