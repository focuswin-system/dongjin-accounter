# 서버 환경변수 예시 — 실제 .env는 git에 포함하지 않는다(server/.env는 gitignore됨).
# 배포 서버에는 이미 생성되어 있음. 새 환경 구성 시 참고용.
DB_HOST=127.0.0.1
DB_USER=dongjin
DB_PASSWORD=__여기에_DB_비밀번호__
DB_NAME=dongjin_acct
PORT=8081
JWT_SECRET=__openssl_rand_-hex_32_로_생성__
